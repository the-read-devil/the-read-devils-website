{
  // Cursor/VS Code: Launch config to run task and open browser
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run: Vite + Browser",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173/",
      "preLaunchTask": "Dev: Vite (5173)",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "Preview: Built site",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173/",
      "preLaunchTask": "Preview: Vite (5173)",
      "webRoot": "${workspaceFolder}"
    }
  ]
}

